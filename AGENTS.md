# SELF-LAYERS 프로젝트 지침

## 적용 범위와 프로젝트 위치

- 이 지침은 `self-layer` 전체에 적용한다. 실제 Next.js 앱과 개발 명령의 실행 위치는 `my-app/`이다.
- 앱 작업 시 `my-app/AGENTS.md`의 Next.js 지침과 작업보고서 규칙도 함께 따른다.
- 원본 자료는 루트의 `project_sources/`, 웹용 문항 데이터는 `my-app/src/data/`, 타입은 `my-app/src/types/survey.ts`에 있다.

## 서비스 목적과 MVP 범위

- SELF-LAYERS는 동기, 관계 반응, 반복되는 내면 규칙, 삶의 가치를 함께 살펴보는 자기이해 검사 웹서비스다.
- 사람을 하나의 유형으로 단정하지 않고, 여러 차원의 조합과 차이를 통해 자기이해와 성찰을 돕는다. 임상적 진단이나 치료적 평가를 제공하는 서비스로 표현하지 않는다.
- 높은 점수를 좋음, 낮은 점수를 나쁨으로 표현하지 않는다.
- 현재 MVP는 검사 응답, 결정론적 채점, 규칙 기반 결과 화면에 집중한다. 별도 요청 전까지 로그인, 결제, 데이터베이스, OpenAI API를 구현하지 않는다.

## 네 영역의 정의

| 영역 | 정의 | 차원 수 | 문항 수 |
|---|---|---:|---:|
| CORE | 나를 움직이는 자동적 동기와 익숙한 반응 방식 | 9 | 36 |
| RELATIONSHIP | 가까운 관계의 변화, 정서적 친밀감과 거리 조절에 대한 반응 방식 | 2 | 8 |
| PATTERN | 압박 속에서 반복되는 믿음과 내면의 규칙 | 8 | 32 |
| DIRECTION | 삶의 선택에서 중요하게 여기는 가치와 향하고 싶은 방향 | 8 | 32 |

- 전체는 정확히 108문항, 27개 차원이며 각 차원에 정확히 4문항이 있다.
- CORE 코드는 `CORE1`~`CORE9`, RELATIONSHIP은 `ANX`, `AVO`다.
- PATTERN은 `AS`, `US`, `DEF`, `SS`, `MIS`, `ED`, `ABN`, `DEP`다.
- DIRECTION은 `GROW`, `AUT`, `ACH`, `AUTH`, `REL`, `SEC`, `EXP`, `CONTR`다.
- 문항 ID는 고유하고 안정적으로 유지한다. 화면 순서는 별도 `displayOrder`로 관리하며 원본의 서비스 노출순서를 따른다. ID 끝 번호나 배열 위치를 응답 식별자로 사용하지 않는다.
- 최종 문항을 사용하며 기존 문항 열의 과거 문구와 혼합하지 않는다.

## 응답척도와 역채점

| 응답값 | 사용자 표시 |
|---|---|
| 1 | 전혀 가깝지 않다 |
| 2 | 별로 가깝지 않다 |
| 3 | 어느 쪽도 아니다 |
| 4 | 꽤 가깝다 |
| 5 | 매우 가깝다 |

- 응답은 정수 1~5만 허용한다. 미응답은 0이나 3으로 대체하지 않는다.
- 원본 역채점 `Y`는 JSON의 `reverse: true`, `N`은 `reverse: false`로 보존한다.
- 보정점수는 역채점 문항에서 `6 - 응답값`, 일반 문항에서 응답값 그대로다. 모든 점수 계산은 보정점수를 사용한다.
- 현재 문항 가중치는 모두 1.0이다.
- 원본의 최종 문구·문항 역할과 역채점 표시에 검토 후보가 존재한다. 문구를 보고 임의로 역채점을 수정하지 않는다. 변경이 필요하면 근거와 검토 후보를 보고하고, 사용자가 확정한 변경만 파생 데이터와 채점 버전에 반영한다.

## 채점과 해석 원칙

- 점수 계산은 결정론적 TypeScript 함수로만 구현한다. 같은 응답과 같은 문항·규칙 버전은 항상 같은 점수와 판정을 반환해야 한다.
- 채점 함수는 UI, 네트워크, AI 호출, 무작위값, 현재 시각에 의존하지 않는 순수 함수로 분리한다. Python 변환 스크립트는 원본 추출·데이터 검증용이며 서비스 채점에 사용하지 않는다.
- 차원 원점수는 해당 차원의 보정점수 평균, 환산점수는 `(평균 - 1) / 4 * 100`, 영역 점수는 해당 영역의 차원 환산점수 평균이다.
- 점수는 규준 확립 전 임시 절대강도다. 백분위, 진단 확률, 전체 성격의 단일 총점으로 표현하지 않는다.
- CORE는 상위 1·2위를 보존하고 차이가 5점 미만이면 근접 여부를 기록한다. 단일 유형을 강제로 확정하지 않는다.
- 대표 PATTERN은 60점 이상인 상위 3개, 대표 DIRECTION은 상위 3개이며 DIRECTION 동점은 공동순위로 다룬다.
- ANX와 AVO는 독립적으로 해석한다. 영역 평균을 계산하더라도 이를 단일 관계 유형으로 환산하지 않는다.
- 차원 내 1개 결측은 응답한 3개 평균, 2개 이상 결측은 해당 차원 산출 보류다. 보류를 0점으로 처리하지 않는다.
- 일률응답과 극단응답의 90% 기준은 응답스타일 주의 표시이며 자동 무효 판정이 아니다.
- 동점의 표시 범위, 결측 차원이 있는 영역 점수, 소수점·강도 경계, 교차해석·반증 조건 등 미확정 정책은 확정된 것처럼 구현하지 않는다. 구현 전 결정 사항을 문서화한다.
- 계산 중간값을 표시용으로 반올림하지 않는다. 표시 정밀도와 판정 기준을 분리한다.
- 결과 리포트 DOCX는 가상 사례의 구조·문체 참고자료다. 샘플 점수, 개인정보, 개인화 문장을 모든 사용자 결과에 적용하지 않는다. 실제 점수는 채점 명세와 코드가 산출한다.

## AI의 역할 제한

- 현재 MVP는 확정된 문구 템플릿과 규칙을 사용한다. AI 연동은 추후 별도 범위다.
- 향후 AI는 TypeScript 규칙엔진이 확정한 결과를 문장으로 표현하는 역할만 맡는다.
- AI가 응답, 역채점, 가중치, 점수, 순위, 차원 판정, 대표 성향, 근접 여부를 생성·수정·재분류해서는 안 된다.
- 교차분석과 반증도 명시적인 규칙으로 확정한다. AI가 근거 없는 조합이나 해석을 추가하거나 반증을 무시해서는 안 된다.
- 반증은 설명의 강도를 낮추거나 설명을 제외하는 데 사용하며 원점수나 CORE 순위를 뒤집지 않는다.
- 계산 결과와 서술을 별도 데이터로 관리한다. AI 출력으로 계산 결과를 덮어쓰지 않는다. 향후 서술 검증 실패 시 확정된 템플릿으로 대체한다.

## 사용자 화면의 용어

- 에니어그램 유형번호 및 유형 라벨(예: `3번 유형`, `유형 3`), `Wing`·`윙`·`날개`, 에니어그램의 `통합`·`분열` 용어를 사용자 화면에 노출하지 않는다.
- `CORE1` 같은 내부 차원 코드는 사용자용 유형명으로 출력하지 않는다. `기준·개선`, `실행·현실화` 등 확정된 차원명을 사용한다.
- 금지 기준은 제목, 본문, 차트 범례, 툴팁, 오류 메시지, 접근성 라벨, 향후 공유·내보내기 결과에도 적용한다.
- 내부 문서와 코드의 식별자는 계산·추적 목적으로 유지할 수 있다. 일반적인 문항 번호나 응답값 1~5는 유형번호와 구분한다.

## 모바일 우선 UI

- 작은 화면을 기준으로 먼저 설계하고 큰 화면으로 확장한다. 검사 흐름에 가로 스크롤이 발생하지 않도록 한다.
- 문항과 5점 응답척도를 읽기 쉽게 표시하고, 터치 영역·선택 상태·키보드 포커스를 명확히 한다. 색상만으로 응답 상태를 구분하지 않는다.
- 진행률, 이전·다음 이동, 응답 수정, 완료 전 미응답 확인을 제공한다. 화면 이동으로 기존 응답이 사라지지 않도록 한다.
- 결과 차트에는 텍스트 이름과 점수를 함께 제공하고 모바일 및 키보드 사용성을 확인한다.

## 원본 자료 보존

- `project_sources/`의 원본 파일은 수정, 덮어쓰기, 삭제하지 않는다. 읽기 전용으로 취급하고 수정이 필요한 산출물은 앱 내부의 파생 파일로 만든다.
- 엑셀 변환 후 원본과 데이터 일치 여부를 검증하고 원본 해시가 유지되는지 확인한다.
- 데이터 구조·역채점·채점 정책의 변경은 근거와 버전을 기록한다. 원본에 없는 규칙을 원본 규칙이라고 기록하지 않는다.

## 작업 후 검증

- 작업 후 앱 디렉터리 `my-app/`에서 lint, typecheck, test를 실행하고 실제 결과를 보고한다.
- lint: `npm.cmd run lint`.
- typecheck 스크립트가 있으면 `npm.cmd run typecheck`, 현재처럼 없으면 `npx.cmd tsc --noEmit --incremental false`를 실행한다.
- test 스크립트가 있으면 `npm.cmd test`를 실행한다. 현재는 test 스크립트와 테스트 러너가 없으므로 그 사실을 보고하고, `npm.cmd run data:validate`로 가능한 데이터 검증을 수행한다. 이를 단위·통합 테스트 통과로 표현하지 않는다.
- 채점·해석 로직 구현 시 테스트 실행 명령을 마련하고 역채점, 경계값, 동점, 결측, 반증, 결과 재현성 테스트를 함께 추가한다.
- 문항 데이터 변경 시 `npm.cmd run data:validate`를 반드시 실행한다. UI 변경 시 모바일·키보드 흐름도 확인한다.
- 검사 실패나 미실행을 숨기지 않으며, 수행하지 않은 검증을 통과했다고 기록하지 않는다.
