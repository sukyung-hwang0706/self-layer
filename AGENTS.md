# SELF-LAYERS 프로젝트 지침

## 적용 범위와 프로젝트 위치

- 이 지침은 `self-layer` 전체에 적용한다. 실제 Next.js 앱과 개발 명령의 실행 위치는 `my-app/`이다.
- 앱 작업 시 `my-app/AGENTS.md`의 Next.js 지침과 작업보고서 규칙도 함께 따른다.
- 원본 자료는 루트의 `project_sources/`, 웹용 문항 데이터는 `my-app/src/data/`, 타입은 `my-app/src/types/survey.ts`에 있다.

## 서비스 목적과 MVP 범위

- SELF-LAYERS는 동기, 현재 상태, 관계 반응, 반복되는 내면 규칙, 스트레스 반응, 삶의 가치를 함께 살펴보는 자기이해 검사 웹서비스다.
- 사람을 하나의 유형으로 단정하지 않고, 여러 Layer의 조합과 차이를 통해 자기이해와 성찰을 돕는다. 임상적 진단이나 치료적 평가를 제공하는 서비스로 표현하지 않는다.
- 높은 점수를 좋음, 낮은 점수를 나쁨으로 표현하지 않는다.
- 현재 MVP는 검사 응답, 결정론적 채점, 규칙 기반 결과 리포트에 집중한다. 별도 요청 전까지 로그인, 결제, 데이터베이스, 무료 간단검사·메일 발송·사람 검토 흐름을 구현하지 않는다. AI 문장화는 `my-app/docs/ai-narrative.md` 범위에서만 사용한다.

## 기준 문서 (V1.0)

- 검사 설계: `project_sources/SELF-LAYERS V1.0 검사 설계서 (85문항) (1).docx` — 파일명과 달리 본문 기준 76문항(Part 1~6 합계 18+9+24+15+9+1)이다.
- 결과 리포트 구조·문체: `project_sources/SELF-LAYERS 결과 리포트 · SAMPLE.html` — 가상 응답자 샘플이다. 샘플 점수·이름·개인화 문장을 실제 사용자 결과에 쓰지 않는다.
- 이전 108문항 자료(`SELF_LAYERS_108문항_검사로직_FINAL_v1.0.xlsx`, `SELF_LAYERS_결과리포트_TEXT_FINAL_v0.61.docx`)는 참고용이며 현재 앱의 기준이 아니다.

## 6 Layer와 6개 Part

| Layer | 질문 | 코드 |
|---|---|---|
| CORE | 무엇이 나를 움직이는가 | T1~T9, SP/SO/SX |
| STATE | 지금 나는 어느 상태에 있는가 | SC, FLX, N-AUT, N-COM, N-REL |
| RELATIONSHIP | 관계에서 어떻게 반응하는가 | ANX, AVO |
| PATTERN | 무엇을 반복하는가 | ABN, ED, MIS, DEF, DEP, SS, AS, US |
| STRESS | 무엇이 나를 흔들고, 그때 무엇을 하는가 | ST1~6, SR, SC1~6 |
| DIRECTION | 무엇을 향해 가고 싶은가 | ACH, AUT, REL, SEC, GROW, AUTH, CONTR, EXP |

| Part | 화면 제목 | 방식 | 문항 |
|---|---|---|---:|
| 1 | 선택 | 3지선다 1·2순위 | 18 |
| 2 | 나를 움직이는 것 | 5점 | 9 |
| 3 | 관계와 나의 규칙 | 5점 | 24 |
| 4 | 흔들리는 순간 | 5점 | 15 |
| 5 | 요즘의 나 (지난 한 달) | 5점 | 9 |
| 6 | 하루의 만족 | 3개 순위 | 1 |

- 문항 은행은 `my-app/src/data/items.ts`, 리포트 문구는 `my-app/src/data/content.ts`, 타입은 `my-app/src/types/assessment.ts`에 있다.
- 문항 ID(E01, M1, A1, S1, ST1, SR1, SC1, P1, CH1)는 영구 식별자다. 화면 순서는 `DISPLAY_ORDER`로 관리하며 Part 1→6 고정, Part 3은 같은 코드 문항을 6문항 이상 떨어뜨린 고정 순서다.
- 5점 척도: 1 전혀 그렇지 않다 · 2 그렇지 않은 편이다 · 3 보통이다 · 4 그런 편이다 · 5 매우 그렇다. 역채점은 설계서의 R 표시만 따른다.

## 채점과 해석 원칙

- 점수 계산은 결정론적 TypeScript 순수 함수(`my-app/src/lib/scoring.ts`)로만 구현한다. UI, 네트워크, AI 호출, 무작위값, 현재 시각에 의존하지 않는다.
- 계산식·임계값은 설계서 10장을 따르며 모두 파일럿 전 가설값이다. 설계서에 식이 없는 V0.2 규칙(Value의 Context Diversity, Influence, 강화·반증 등)은 `my-app/docs/scoring-decisions.md`에 임시 규칙으로 기록했다. 원본에 없는 규칙을 원본 규칙이라고 기록하지 않는다.
- Core는 상황선택형(ipsative)이므로 TypeScore를 인구집단 백분위로 표현하지 않는다. Core와 Value는 같은 Part 1 응답에서 나오므로 서로를 검증한 것으로 쓰지 않는다.
- Primary 동점은 TypeScore → M(Tn) → 1순위 횟수로 가리고, 모두 같으면 판정 유보로 표시하며 단정 문장을 쓰지 않는다.
- ANX와 AVO는 독립적으로 해석하며 단일 관계 유형 점수로 합치지 않는다.
- 반증은 설명의 강도를 낮추거나 설명을 제외하는 데만 쓰며 점수나 Core 순위를 바꾸지 않는다.
- STATE는 지난 한 달 기준의 현재 위치다. 성격으로 표현하지 않는다.
- 듣고 싶었던 메시지는 어린 시절 사건의 근거로 쓰지 않고 성찰 언어로만 쓴다.
- 계산 중간값은 반올림하지 않는다. 표시 정밀도와 판정 기준을 분리한다.

## AI의 역할 제한

- AI 문장화는 `my-app/src/app/api/narrative/route.ts`와 `my-app/src/lib/narrative.ts`에 있다. 사용자가 결과 화면에서 직접 요청할 때만 호출한다.
- AI는 TypeScript 규칙엔진이 확정한 결과를 문장으로 표현하는 역할만 맡는다. 응답, 역채점, 가중치, 점수, 순위, Core 판정, 밴드, 교차 규칙을 생성·수정·재분류해서는 안 된다.
- 서버는 받은 응답으로 다시 채점하며 클라이언트가 보낸 점수를 쓰지 않는다. 모델에는 확정된 사실만 보내고 원응답·이름·내부 코드를 보내지 않는다.
- AI 출력은 `validateNarrative`를 통과한 문단만 쓰고, 실패한 문단은 확정된 템플릿으로 대체한다. 계산 결과와 서술은 별도 데이터로 관리한다.
- 반증은 설명의 강도를 낮추거나 설명을 제외하는 데 사용하며 원점수나 Core 순위를 뒤집지 않는다.
- 검증 규칙이나 프롬프트를 바꾸면 `NARRATIVE_VERSION`을 올리고 `narrative.test.ts`를 갱신한다.

## 사용자 화면의 용어

- 에니어그램 유형번호 및 유형 라벨(예: `3번 유형`, `유형 3`), `Wing`·`윙`·`날개`, 에니어그램의 `통합`·`분열` 용어를 사용자 화면에 노출하지 않는다. 통합·분열은 Expansion Mode / Protection Mode, Wing은 Influence로 쓴다.
- `T1` 같은 내부 코드는 사용자 화면에 출력하지 않는다. Core 이름은 ORDER · CARE · DRIVE · DEPTH · INSIGHT · TRUST · SPARK · STAND · FLOW를 사용한다.
- Riso-Hudson 유형명, RHETI·WEPSS·YSQ·ECR·PSS 등 타 검사 문항의 번역을 쓰지 않는다.
- 금지 기준은 제목, 본문, 차트 범례, 툴팁, 오류 메시지, 접근성 라벨, 향후 공유·내보내기 결과에도 적용한다.
- 내부 문서와 코드의 식별자는 계산·추적 목적으로 유지할 수 있다. 일반적인 문항 번호나 응답값 1~5는 유형번호와 구분한다.

## 모바일 우선 UI

- 작은 화면을 기준으로 먼저 설계하고 큰 화면으로 확장한다. 검사 흐름에 가로 스크롤이 발생하지 않도록 한다.
- 문항과 5점 응답척도를 읽기 쉽게 표시하고, 터치 영역·선택 상태·키보드 포커스를 명확히 한다. 색상만으로 응답 상태를 구분하지 않는다.
- 진행률, 이전·다음 이동, 응답 수정, 완료 전 미응답 확인을 제공한다. 화면 이동으로 기존 응답이 사라지지 않도록 한다.
- 결과 차트에는 텍스트 이름과 점수를 함께 제공하고 모바일 및 키보드 사용성을 확인한다.

## 원본 자료 보존

- `project_sources/`의 원본 파일은 수정, 덮어쓰기, 삭제하지 않는다. 읽기 전용으로 취급하고 수정이 필요한 산출물은 앱 내부의 파생 파일로 만든다.
- 문항 은행은 설계서 원문과 일치해야 하며, `npm.cmd test`의 문항 은행 테스트(문항 수, 유형·가치 등장 횟수, 역채점, Part 3 간격)로 확인한다.
- 데이터 구조·역채점·채점 정책의 변경은 근거와 버전을 기록한다. 원본에 없는 규칙을 원본 규칙이라고 기록하지 않는다.

## 작업 후 검증

- 작업 후 앱 디렉터리 `my-app/`에서 lint, typecheck, test를 실행하고 실제 결과를 보고한다.
- lint: `npm.cmd run lint` · typecheck: `npm.cmd run typecheck` · 단위 테스트: `npm.cmd test` · E2E: `npm.cmd run test:e2e`.
- 채점·해석 로직을 바꾸면 역채점, 경계값, 동점, 반증, 결과 재현성 테스트를 함께 갱신하고 `SCORING_VERSION`을 올린다.
- UI 변경 시 모바일·키보드 흐름과 가로 스크롤 여부를 확인한다.
- 검사 실패나 미실행을 숨기지 않으며, 수행하지 않은 검증을 통과했다고 기록하지 않는다.
